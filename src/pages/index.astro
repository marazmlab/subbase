---
import Layout from "@/layouts/Layout.astro";
import { DashboardLayout } from "@/components/dashboard/DashboardLayout";

// Middleware handles authentication and redirects automatically:
// - Unauthenticated users are redirected to /login
// - Authenticated users can access this page
// - User data is available in Astro.locals.user
---

<Layout title="Dashboard - Subbase">
  <DashboardLayout client:load />
</Layout>
